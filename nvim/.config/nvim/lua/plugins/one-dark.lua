return {
    -- "navarasu/onedark.nvim",
    -- config = function()
    --     math.randomseed(os.time())
    --     local schemes = { "dark", "darker", "cool", "deep", "warm", "warmer" }
    --     local selected = math.random(#schemes)

    --     require("onedark").setup {
    --         style = schemes[selected]
    --     }
    --     require("onedark").load()
    -- end
}
